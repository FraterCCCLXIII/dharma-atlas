export default function LocationsLoading() {
  return (
    <div className="flex h-[calc(100dvh-var(--site-nav-height))] items-center justify-center bg-surface-elevated">
      <p className="text-sm text-ink-muted">Loading directory…</p>
    </div>
  );
}
